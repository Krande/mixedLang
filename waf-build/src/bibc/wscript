def options(opt):
    opt.load('compiler_c')


def configure(conf):
    conf.load('compiler_c')


def build(bld):
    bld(
        features="c cshlib",
        name="asterbibc",
        source='sdlprog.c',
        target='bibc',
    )
from waflib import Build


def options(opt):
    opt.load('compiler_c')


def configure(conf):
    conf.load('compiler_c')


def build(ctx: Build.InstallContext):
    ctx(
        features="c cshlib",
        name="asterbibc",
        source='sdlprog.c',
        target='bibc',
    )
